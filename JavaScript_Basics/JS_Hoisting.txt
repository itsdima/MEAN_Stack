1) Undefined
2) magnet -- because we never call 'needle' outside of the function. and function gets scoped to the top of the page so when we call for it, it gets ran
3) super cool (because we never call for the function 
4) chicken, half-chicken
5) (syntax error)(because food only exists within the function)
6) undefined, rock, r&b, disco
7) san jose, seattle, burbank, san jose (because dojo becomes a global variable without declaring it (implicit declaration) which is not recommended)
